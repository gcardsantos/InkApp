<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:local="clr-namespace:InkApp.Gestos"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             NavigationPage.HasNavigationBar="False"
             x:Class="InkApp.Views.ImagePage"
             BackgroundColor="Transparent">
    <ContentPage.Content>
        <RelativeLayout Padding="20" BindingContext="{Binding Item}">
            <local:PinchGesture>
                <local:PinchGesture.Content>
                    <ff:CachedImage Source="{Binding ImageHigh}"/>
                </local:PinchGesture.Content>
            </local:PinchGesture>
            <Button Text="Salvar" Command="{Binding PhotoCommand}"/>
        </RelativeLayout>           
        
    </ContentPage.Content>
    
</ContentPage>