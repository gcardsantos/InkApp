<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                  prism:ViewModelLocator.AutowireViewModel="True"
                  x:Class="InkApp.Views.TopMasterDetailPage"
                  MasterBehavior="SplitOnPortrait">

    <MasterDetailPage.Master>
        <ContentPage Title="Menu">
            <ScrollView>
                <StackLayout Orientation="Vertical" Padding="70" BackgroundColor="Gainsboro" VerticalOptions="Center" WidthRequest="50">
                    <Frame HeightRequest="100" BorderColor="WhiteSmoke" HorizontalOptions="CenterAndExpand">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Instagram}" FontSize="72" HorizontalOptions="Center"/>
                    </Frame>
                    <Label TextColor="Black"  FontSize="20">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="{StaticResource Home}" FontFamily="{DynamicResource MaterialButton}" FontSize="27"/>
                                <Span Text=" "/>
                                <Span Text="Home"/>
                            </FormattedString>
                        </Label.FormattedText>
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/HomePage"/>
                        </Label.GestureRecognizers>
                    </Label>

                    <Label TextColor="Black" Margin="0, 10" FontSize="20">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="{StaticResource Feed}" FontFamily="{DynamicResource MaterialButton}" FontSize="27"/>
                                <Span Text=" "/>
                                <Span Text="Novidades"/>
                            </FormattedString>
                        </Label.FormattedText>
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/FeedPage"/>
                        </Label.GestureRecognizers>
                    </Label>

                    <Label FontSize="20" TextColor="Black" Margin="0, 10">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="{StaticResource SavePhotos}" FontFamily="{DynamicResource MaterialButton}" FontSize="27"/>
                                <Span Text=" "/>
                                <Span Text="Tatuadores"/>
                            </FormattedString>
                        </Label.FormattedText>
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/BasePeoplePage"/>
                        </Label.GestureRecognizers>
                    </Label>

                    <Label  FontSize="20" TextColor="Black">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="{StaticResource HeartBlack}" FontFamily="{DynamicResource MaterialButton}" FontSize="27"/>
                                <Span Text=" "/>
                                <Span Text="Preferidas"/>
                            </FormattedString>
                        </Label.FormattedText>
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/SavePhotosPage"/>
                        </Label.GestureRecognizers>
                    </Label>                    
                    <StackLayout Padding="20">
                        <Label Text="Deseja ser divulgado?" FontAttributes="Bold" VerticalOptions="End" HorizontalOptions="Center" IsVisible="{Binding BtnEnabled}"/>
                        <Label Text="Entre em contato conosco" VerticalOptions="End" HorizontalOptions="Center" IsVisible="{Binding BtnEnabled}"/>
                        <Button Text="Solicitar" Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/RequestPage" BackgroundColor="White" CornerRadius="30" BorderColor="WhiteSmoke" Padding="1"/>
                    </StackLayout>
                </StackLayout>
            </ScrollView>            
        </ContentPage>
    </MasterDetailPage.Master>
</MasterDetailPage>