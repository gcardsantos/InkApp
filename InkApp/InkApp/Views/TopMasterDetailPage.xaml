<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                  prism:ViewModelLocator.AutowireViewModel="True"
                  x:Class="InkApp.Views.TopMasterDetailPage"
                  MasterBehavior="SplitOnPortrait">

    <MasterDetailPage.Master>
        <ContentPage Title="Menu">
            <ScrollView>
                <StackLayout Orientation="Vertical" Padding="50" BackgroundColor="Gainsboro" VerticalOptions="Center" WidthRequest="50">
                    <Frame HeightRequest="200" BorderColor="WhiteSmoke" HorizontalOptions="CenterAndExpand">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Instagram}" FontSize="72" HorizontalOptions="Center"/>
                    </Frame>
                    <StackLayout Orientation="Horizontal" VerticalOptions="Start" HorizontalOptions="CenterAndExpand">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Home}" FontSize="Medium" TextColor="Black" Margin="0, 10"/>
                        <Button Text="Home" Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/HomePage" BackgroundColor="Transparent" Padding="1"/>
                    </StackLayout>
                    <StackLayout Orientation="Horizontal" VerticalOptions="Center" HorizontalOptions="CenterAndExpand">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Feed}" FontSize="Medium" TextColor="Black" Margin="0, 10"/>
                        <Button Text="Feed" Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/FeedPage" BackgroundColor="Transparent" Padding="1"/>
                    </StackLayout>
                    <StackLayout Orientation="Horizontal" VerticalOptions="Center" HorizontalOptions="CenterAndExpand">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource SavePhotos}" FontSize="Medium" TextColor="Black" Margin="0, 10"/>
                        <Button  Text="Photos" Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/SavePhotosPage" BackgroundColor="Transparent" Padding="1"/>
                    </StackLayout>                    
                    
                    <StackLayout Padding="20">
                        <Label Text="Deseja ser divulgado?" FontAttributes="Bold" VerticalOptions="End" HorizontalOptions="Center" IsVisible="{Binding BtnEnabled}"/>
                        <Label Text="Entre em contato conosco" VerticalOptions="End" HorizontalOptions="Center" IsVisible="{Binding BtnEnabled}"/>
                        <Button Text="Solicitar" Command="{Binding NavigateCommand}" CommandParameter="CustomNavigationPage/RequestPage" BackgroundColor="White" CornerRadius="30" BorderColor="WhiteSmoke" Padding="1"/>
                    </StackLayout>
                </StackLayout>
            </ScrollView>            
        </ContentPage>
    </MasterDetailPage.Master>
</MasterDetailPage>