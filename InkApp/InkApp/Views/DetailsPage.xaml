<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:abstractions="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             xmlns:fl="clr-namespace:DLToolkit.Forms.Controls;assembly=DLToolkit.Forms.Controls.FlowListView"
             xmlns:extend="clr-namespace:Xamarin.Forms.Extended;assembly=Xamarin.Forms.Extended.InfiniteScrolling"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="InkApp.Views.DetailsPage"
             Title="{Binding Title}"
             NavigationPage.HasNavigationBar="True">

    <ScrollView Padding="10">
        <StackLayout Orientation="Vertical">
            
            <Frame BorderColor="WhiteSmoke" HasShadow="True" CornerRadius="10" Padding="10" Visual="Material">
                <StackLayout Orientation="Vertical" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                    <StackLayout Orientation="Vertical" VerticalOptions="CenterAndExpand" Margin="25, 25, 25, 0">
                        <abstractions:CircleImage BorderColor="Black" Source="{Binding ProfileImage}" Scale="2"/>
                        <Label Text="{Binding Title}" FontSize="Large" FontAttributes="Bold" TextColor="Black" Margin="0, 19, 0, 0"/>
                    </StackLayout>
                    <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Location}" Margin="2" TextColor="Black"/>
                        <Label Text="{Binding Local}" FontSize="15" TextColor="Black"/>
                    </StackLayout>
                    <StackLayout Orientation="Horizontal">
                        <RelativeLayout Padding="10" VerticalOptions="Center" WidthRequest="15" HeightRequest="15">
                            <Button Command="{Binding BtnLocal}" FontSize="Micro" BackgroundColor="Transparent" BorderColor="WhiteSmoke" BorderWidth="2" CornerRadius="10"/>
                            <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Location}" TextColor="Black"  Scale="1.5" Margin="13, 10"/>
                        </RelativeLayout>
                        <RelativeLayout Padding="10" VerticalOptions="Center" WidthRequest="15" HeightRequest="15">
                            <Button Command="{Binding BtnIg}" FontSize="Micro" BackgroundColor="Transparent" BorderColor="WhiteSmoke" BorderWidth="2" CornerRadius="10"/>
                            <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Instagram}" TextColor="Black" Scale="1.5" Margin="15, 10"/>
                        </RelativeLayout>
                        <RelativeLayout Padding="10" VerticalOptions="Start"  WidthRequest="15" HeightRequest="15">
                            <Button Command="{Binding BtnWhats}" BackgroundColor="Transparent"  BorderColor="WhiteSmoke" BorderWidth="2" CornerRadius="10"/>
                            <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Whats}" TextColor="Black" Scale="1.5" Margin="14, 10"/>
                        </RelativeLayout>
                        <RelativeLayout Padding="10" VerticalOptions="Center" WidthRequest="15" HeightRequest="15">
                            <Button Command="{Binding BtnFace}"  BackgroundColor="Transparent" BorderColor="WhiteSmoke" BorderWidth="2" CornerRadius="10"/>
                            <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Facebook}" TextColor="Black" Scale="1.5" Margin="14, 10"/>
                        </RelativeLayout>
                    </StackLayout>
                    <Label Text="{Binding Sobre}" FontSize="Micro" VerticalOptions="Center" HorizontalOptions="Center"/>
                </StackLayout>
            </Frame>
            <Label Text="Não foi possível conectar." IsVisible="{Binding Visible}" FontSize="Large"/>
            <fl:FlowListView x:Name="flowListView" FlowColumnCount="3" FlowItemsSource="{Binding Feed}" RowHeight="150" SeparatorVisibility="None" HasUnevenRows="false"
			    FlowItemTappedCommand="{Binding PhotoTappedCommand}" FlowLastTappedItem="{Binding LastTappedItem}"
                FlowLoadingCommand="{Binding LoadingCommand}" FlowIsLoadingInfiniteEnabled="True" FlowIsLoadingInfinite="{Binding IsLoadingInfinite}">
                <fl:FlowListView.FlowColumnTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="0" HasShadow="True" Padding="5">
                            <Grid VerticalOptions="Fill" HorizontalOptions="CenterAndExpand">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="150"/>
                                </Grid.RowDefinitions>
                                <ff:CachedImage DownsampleToViewSize="True" Source="{Binding ImageLow}"/>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </fl:FlowListView.FlowColumnTemplate>
                <fl:FlowListView.Footer>
                    <Grid Padding="6" IsVisible="{Binding IsBusy}">
                        <Grid.Triggers>
                            <Trigger TargetType="Grid" Property="IsVisible" Value="False">
                                <Setter Property="HeightRequest" Value="0" />
                            </Trigger>
                        </Grid.Triggers>
                        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                                       Color="Black" VerticalOptions="Center" 
                                       HorizontalOptions="Center"/>
                    </Grid>
                </fl:FlowListView.Footer>
            </fl:FlowListView>
            
            <Button Command="{Binding LoadingCommand}" Text="Carregar Mais" IsVisible="True"/>

        </StackLayout>
    </ScrollView>
</ContentPage>