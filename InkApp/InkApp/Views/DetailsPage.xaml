<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:abstractions="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             xmlns:c="clr-namespace:InkApp.Converters"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="InkApp.Views.DetailsPage"
             Title="{Binding Title}"
             NavigationPage.HasNavigationBar="True">

    <ScrollView Padding="10">
        <StackLayout>
            <Label Text="Não foi possível conectar." IsVisible="{Binding Visible}"/>
            <Frame BorderColor="WhiteSmoke" HasShadow="True" CornerRadius="10" Padding="10">
                <Grid VerticalOptions="Fill">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="0.6*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <abstractions:CircleImage Grid.Row="1" BorderColor="Black" Source="{Binding ProfileImage}"/>
                    <Label Grid.Column="1" Grid.Row="0" Text="{Binding Title}" FontSize="Large" FontAttributes="Bold" TextColor="Black" VerticalOptions="Start"/>
                    <StackLayout Grid.Column="1" Grid.Row="1" Orientation="Horizontal" VerticalOptions="Center">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Location}" Margin="2" TextColor="Black"/>
                        <Label Text="{Binding Local}" FontSize="15" TextColor="Black"/>
                    </StackLayout>
                    <RelativeLayout Grid.Column="2" Grid.Row="0" Padding="10" VerticalOptions="Center" WidthRequest="20" HeightRequest="20">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Instagram}" TextColor="Black" Scale="1.5" Margin="13"/>
                        <Button Command="{Binding BtnIg}" FontSize="Micro" BackgroundColor="Transparent"/>
                    </RelativeLayout>
                    <RelativeLayout Grid.Column="2" Grid.Row="1" Padding="10"  WidthRequest="20" HeightRequest="20">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Whats}" TextColor="Black" Scale="1.5" Margin="13, 33, 0, 0"/>
                        <Button Command="{Binding BtnWhats}" BackgroundColor="Transparent"/>
                    </RelativeLayout>
                    <RelativeLayout Grid.Column="2"  Grid.Row="2" Padding="10" VerticalOptions="Center" WidthRequest="20" HeightRequest="20">
                        <Label FontFamily="{DynamicResource MaterialButton}" Text="{StaticResource Facebook}" TextColor="Black" Scale="1.5" Margin="13"/>
                        <Button Command="{Binding BtnFace}"  BackgroundColor="Transparent"/>
                    </RelativeLayout>
                    <Label Grid.Column="1" Grid.Row="2" Text="{Binding Sobre}" FontSize="Micro" VerticalOptions="Start"/>
                </Grid>
                
            </Frame>
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="Black"/>
            <CollectionView ItemsSource="{Binding Feed}" SelectionMode="Single" SelectionChanged="CollectionView_SelectionChanged">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="3"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="0" HasShadow="True" Padding="5">
                            <Grid VerticalOptions="Fill" HorizontalOptions="CenterAndExpand">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="125"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="125"/>
                                </Grid.RowDefinitions>
                                <Image Grid.RowSpan="3" Aspect="AspectFill">
                                    <Image.Source>
                                        <UriImageSource Uri="{Binding ImageLow}"/>
                                    </Image.Source>
                                </Image>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ScrollView>
</ContentPage>