<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:fl="clr-namespace:DLToolkit.Forms.Controls;assembly=DLToolkit.Forms.Controls.FlowListView"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:cards="clr-namespace:PanCardView;assembly=PanCardView"
             xmlns:controls="clr-namespace:PanCardView.Controls;assembly=PanCardView"
             xmlns:expandable="clr-namespace:Expandable;assembly=ExpandableView"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="InkApp.Views.FeedPage"
             BackgroundColor="White"
             Title="Novidades">

    <ScrollView Orientation="Vertical" BackgroundColor="AntiqueWhite">
        <StackLayout Orientation="Vertical" Padding="0">
            <ScrollView Orientation="Horizontal" MinimumHeightRequest="60">
                <StackLayout Padding="10" Orientation="Horizontal" MinimumHeightRequest="50">
                    <Frame Padding="5" CornerRadius="10" BorderColor="Black" MinimumHeightRequest="30">
                        <Label Text="Todas" FontSize="Default" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding FilterCommand}" CommandParameter="All"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                    <Frame Padding="5" CornerRadius="10" BorderColor="Black" MinimumHeightRequest="30">
                        <Label Text="BlackWork" FontSize="Default" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding FilterCommand}" CommandParameter="blackwork"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                    <Frame Padding="5" CornerRadius="10" BorderColor="Black" MinimumHeightRequest="30">
                        <Label Text="Black and Gray" FontSize="Default" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding FilterCommand}" CommandParameter="blackandgray"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                    <Frame Padding="5" CornerRadius="10" BorderColor="Black" MinimumHeightRequest="30">
                        <Label Text="Fine Line" FontSize="Default" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding FilterCommand}" CommandParameter="fineline"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                    <Frame Padding="5" CornerRadius="10" BorderColor="Black" MinimumHeightRequest="30">
                        <Label Text="Geometric" FontSize="Default" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding FilterCommand}" CommandParameter="geometric"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                    <Frame Padding="5" CornerRadius="10" BorderColor="Black" MinimumHeightRequest="30">
                        <Label Text="Old School" FontSize="Default" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding FilterCommand}" CommandParameter="oldschool"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                    <Frame Padding="5" CornerRadius="10" BorderColor="Black" MinimumHeightRequest="30">
                        <Label Text="Tribal" FontSize="Default" VerticalOptions="Center" HorizontalOptions="Center" FontAttributes="Bold">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding FilterCommand}" CommandParameter="tribal"/>
                            </Label.GestureRecognizers>
                        </Label>
                    </Frame>
                </StackLayout>
            </ScrollView>

            <!--<cards:CarouselView 
                ItemsSource="{Binding TopList}"
                SelectedIndex="{Binding Position}" 
                Padding="20" HorizontalOptions="Center">
                <cards:CarouselView.ItemTemplate>
                    <DataTemplate>
                        <ContentView>
                            <Frame VerticalOptions="Center" HorizontalOptions="Center" HeightRequest="200" WidthRequest="200" HasShadow="True" IsClippedToBounds="true" BackgroundColor= "WhiteSmoke" CornerRadius="10" Padding="1">
                                <ImageButton Source="{Binding ImageLow}" Aspect="AspectFit" Command="{Binding TopCommand}"/>
                            </Frame>
                        </ContentView>
                    </DataTemplate>
                </cards:CarouselView.ItemTemplate>

                <controls:IndicatorsControl/>
            </cards:CarouselView>-->

            <CollectionView x:Name="dataCollection" ItemsSource="{Binding Feed}" SelectedItem="{Binding LastTappedItem}" ItemSizingStrategy="MeasureFirstItem"
                            SelectionChangedCommand="{Binding PhotoTappedCommand}" SelectionMode="Single" ItemsUpdatingScrollMode="KeepItemsInView"
                            RemainingItemsThresholdReachedCommand="{Binding LoadingCommand}" RemainingItemsThreshold="20" IsVisible="{Binding CollectionVisible}">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="3" HorizontalItemSpacing="2" VerticalItemSpacing="2"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="150"/>
                            </Grid.RowDefinitions>
                            <ff:CachedImage DownsampleToViewSize="True" Source="{Binding ImageLow}" CacheDuration="30"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                                           Color="Black" VerticalOptions="Center" 
                                           HorizontalOptions="Center"/>
            <!--<fl:FlowListView x:Name="flowListView" FlowColumnCount="3" FlowItemsSource="{Binding Feed}" RowHeight="150" SeparatorVisibility="Default" SeparatorColor="White"
			    FlowItemTappedCommand="{Binding PhotoTappedCommand}" FlowLastTappedItem="{Binding LastTappedItem}" HasUnevenRows="False" FlowUseAbsoluteLayoutInternally="True">
                <fl:FlowListView.FlowColumnTemplate>
                    <DataTemplate>
                        <Grid VerticalOptions="Fill" HorizontalOptions="CenterAndExpand">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="150"/>
                            </Grid.RowDefinitions>
                            
                            <ff:CachedImage Source="{Binding ImageLow}"/>
                        </Grid>
                    </DataTemplate>
                </fl:FlowListView.FlowColumnTemplate>
                <fl:FlowListView.Footer>
                    <StackLayout Orientation="Vertical">
                        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" 
                                       Color="Black" VerticalOptions="Center" 
                                       HorizontalOptions="Center"/>
                        <ImageButton Scale="0.3" Source="loadmore.png" HeightRequest="50" BackgroundColor="White" Command="{Binding LoadingCommand}" IsVisible="{Binding IsLoadMore}"/>
                    </StackLayout>
                </fl:FlowListView.Footer>
            </fl:FlowListView>-->
        </StackLayout>
    </ScrollView>
</ContentPage>